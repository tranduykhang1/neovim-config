local options = {
  ensure_installed = { "lua", "vim", "vimdoc", 'typescript'},

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = false },
}

return options
